package TileMap;


public class MapLayer extends poj.EngineState
{
	/**
	 * Constructs the MapLayer class, which is a nice wrapper for using a
	 * specific map layer
	 *
	 * @param  CAPACITY	creates the map
	 */
	public MapLayer(int CAPACITY)
	{
		super(CAPACITY);
	}
}
